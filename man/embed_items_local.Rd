% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generation_embeddings_local.R
\name{embed_items_local}
\alias{embed_items_local}
\title{Generate Raw Embeddings Using Local Models}
\usage{
embed_items_local(
  embedding.model,
  items,
  pooling_strategy = "mean",
  device = "auto",
  batch_size = 32,
  max_length = 512,
  silently = FALSE
)
}
\arguments{
\item{embedding.model}{Character string specifying either:
\itemize{
\item A HuggingFace model identifier for BERT-style models:
\itemize{
\item "bert-base-uncased" (768 dims)
\item "roberta-base" (768 dims)
\item "microsoft/deberta-v3-base" (768 dims)
\item "BAAI/bge-base-en-v1.5" (768 dims) - if loaded for raw extraction
}
\item A local path to a saved model
}}

\item{items}{Data frame containing the items to embed. Must have two columns:
\itemize{
\item \code{statement}: Character vector of text to embed
\item \code{ID}: Unique identifiers for each statement
}}

\item{pooling_strategy}{Character string for pooling token embeddings:
\itemize{
\item "mean": Average all token embeddings (default)
\item "cls": Use only the CLS token embedding
\item "max": Max pooling across tokens
}}

\item{device}{Character string specifying computation device}

\item{batch_size}{Integer. Number of items to process simultaneously}

\item{max_length}{Integer. Maximum sequence length (default 512)}

\item{silently}{Logical. If FALSE, displays progress messages}
}
\value{
A list with embeddings matrix and success flag

Generate Raw Embeddings Using Local Models

A list with embeddings matrix and success flag

Generate Raw Embeddings Using Local Models

A list with embeddings matrix and success flag
}
\description{
Generates raw feature embeddings for text items using locally installed
transformer models. These are raw embeddings from the model's encoder,
not similarity-optimized embeddings.

Generates raw feature embeddings for text items using locally installed
transformer models. These are raw embeddings from the model's encoder,
not similarity-optimized embeddings.

Generates raw feature embeddings for text items using locally installed
transformer models. These are raw embeddings from the model's encoder,
not similarity-optimized embeddings.
}
