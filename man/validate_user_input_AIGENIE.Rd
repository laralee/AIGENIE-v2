% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/user_validation.R
\name{validate_user_input_AIGENIE}
\alias{validate_user_input_AIGENIE}
\title{Validate All User Inputs for AI-GENIE}
\usage{
validate_user_input_AIGENIE(
  item.attributes,
  openai.API,
  hf.token,
  main.prompts,
  groq.API,
  anthropic.API,
  jina.API,
  model,
  temperature,
  top.p,
  embedding.model,
  target.N,
  domain,
  scale.title,
  item.examples,
  audience,
  item.type.definitions,
  response.options,
  prompt.notes,
  system.role,
  EGA.model,
  EGA.algorithm,
  EGA.uni.method,
  keep.org,
  items.only,
  embeddings.only,
  adaptive,
  run.overall,
  all.together,
  plot,
  silently
)
}
\arguments{
\item{item.attributes}{A named list of attributes and item types. Must be validated via
\code{item.attributes_validate()}.}

\item{openai.API}{A string. OpenAI API key.}

\item{hf.token}{A string. HuggingFace API key.}

\item{main.prompts}{A named list of custom prompts that the user specifies (if desired)}

\item{groq.API}{A string or NULL. Groq API key.}

\item{model}{A string. The user-specified language model. Will be resolved to a
canonical model name using \code{normalize_model_name()}.}

\item{temperature}{A numeric value between 0 and 2.}

\item{top.p}{A numeric value between 0 and 1.}

\item{embedding.model}{A string or NULL. Must be one of the accepted OpenAI embedding models.}

\item{target.N}{Either a scalar integer, NULL, or a named list/vector of integers
corresponding to each attribute. Used for synthetic item generation.}

\item{domain}{A string describing the domain of the assessment.}

\item{scale.title}{A string naming the scale.}

\item{item.examples}{A data frame containing \code{type}, \code{attribute}, and \code{statement} columns.
All values must be strings. Optional.}

\item{audience}{A string or NULL. The intended audience of the assessment.}

\item{item.type.definitions}{A named list mapping item types to their descriptions.
Optional.}

\item{response.options}{An atomic vector of strings listing the response options users will have.
Optional.}

\item{prompt.notes}{A named list or string that gives the LLM additional instructions to be appended to the prompt.
Optional.}

\item{system.role}{A string or NULL. Used to customize the system prompt.}

\item{EGA.model}{A string or NULL. One of \code{"BGGM"}, \code{"glasso"}, or \code{"TMFG"}.}

\item{EGA.algorithm}{A string. One of \code{"leiden"}, \code{"louvain"}, or \code{"walktrap"}.}

\item{EGA.uni.method}{A string. One of \code{"expand"}, \code{"LE"}, or \code{"louvain"}.}

\item{keep.org}{A boolean. If TRUE, preserve original inputs in the output.}

\item{items.only}{A boolean. Whether to generate only items.}

\item{embeddings.only}{A boolean. Whether to run in embedding-only mode.}

\item{adaptive}{A boolean. Whether adaptive design logic should be applied.}

\item{plot}{A boolean. Whether to display plots for visual diagnostics.}

\item{silently}{A boolean. If TRUE, suppresses warning messages.}
}
\value{
A named list containing:
\describe{
\item{target.N}{A named list of integers, aligned with \code{item.attributes}}
\item{EGA.model}{Canonical model string or NULL}
\item{EGA.uni.method}{Canonical unidimensionality method}
\item{EGA.algorithm}{Canonical community detection algorithm}
\item{model}{Resolved model string for text generation}
\item{item.type.definitions}{Cleaned item type definitions (if provided)}
\item{item.examples}{Cleaned item examples (if provided)}
\item{item.attributes}{Cleaned and normalized item attributes}
\item{prompt.notes}{Cleaned and normalized prompt notes (if provided)}
\item{main.prompts}{Cleaned and normalized main prompts (if provided)}
\item{custom}{A flag signaling whether we are in custom mode or not}
}
}
\description{
This function performs comprehensive validation and normalization of all user-supplied
inputs to the AI-GENIE package. It checks logical flags, strings, model names, item
attribute structures, and ensures consistency across all interdependent components.
}
\details{
If any input is invalid or misaligned with the package’s expected structure,
informative errors or warnings are raised. Cleaned and normalized objects are returned
for use downstream.
}
