% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline_helpers_optimized.R
\name{build_filtering_audit}
\alias{build_filtering_audit}
\title{Build GENIE item-filtering audit table}
\usage{
build_filtering_audit(
  items,
  type_name,
  uva_log,
  boot_removed,
  initial_ega,
  uva.cut.off,
  stability.cut.off = 0.75,
  selection_dropped = character(0),
  final_dropped = character(0)
)
}
\arguments{
\item{items}{Data frame for one item type with \code{ID}, \code{statement}, and \code{attribute}.}

\item{type_name}{Character label for the item type.}

\item{uva_log}{Item-level removal log returned by \code{reduce_redundancy_uva()}.}

\item{boot_removed}{Data frame of removals returned by \code{iterative_stability_check()}.}

\item{initial_ega}{Pre-reduction EGA object computed on the full dense embeddings.}

\item{uva.cut.off}{Numeric wTO cutoff used by UVA.}

\item{stability.cut.off}{Numeric item-stability cutoff used by bootEGA.}

\item{selection_dropped}{Character vector of items left unassigned during embedding/model selection.}

\item{final_dropped}{Character vector of items left unassigned by the final EGA.}
}
\value{
A tidy data frame with one row per filtered item and the evidence for
its removal.
}
\description{
Combines the stage-specific evidence that caused item removal with
pre-reduction network-loading diagnostics. UVA rows report wTO redundancy
evidence and the retained redundant partner. bootEGA rows report empirical
item stability. Network loadings are descriptive context and are not used as
filtering thresholds.
}
\keyword{internal}
