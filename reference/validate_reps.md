# Check that reps is an integer

Check that reps is an integer

## Usage

``` r
validate_reps(reps)
```

## Arguments

- reps:

  The number of repetitions per prompt requested

## Value

if valid, the `reps` value as an integer object type
