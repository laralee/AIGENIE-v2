# Embed Items Using Sentence-Transformers Library

Embed Items Using Sentence-Transformers Library

## Usage

``` r
embed_items_via_sentence_transformers(
  embedding.model,
  items,
  hf.token = NULL,
  silently = FALSE
)
```
