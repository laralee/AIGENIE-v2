# Combine filtering audits across item types

Combine filtering audits across item types

## Usage

``` r
combine_filtering_audits(item_type_level)
```

## Arguments

- item_type_level:

  Named list of item-type pipeline results.

## Value

A single data frame containing all available item-level filtering audit
rows across item types.
