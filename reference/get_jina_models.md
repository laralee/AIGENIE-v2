# Get Jina AI Embedding Models

Returns a curated list of Jina AI embedding models. Jina does not
provide a model listing API endpoint, so this list is maintained
manually.

## Usage

``` r
get_jina_models()
```

## Value

Data frame of Jina AI embedding models
